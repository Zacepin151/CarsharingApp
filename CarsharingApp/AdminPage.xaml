<Page x:Class="CarsharingApp.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CarsharingApp"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminPage">

    <Page.Background>
        <SolidColorBrush Color="LightGray"/>
    </Page.Background>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Пользователи"
               FontSize="24"
               FontWeight="Bold"
               HorizontalAlignment="Center"
               Margin="0,10" 
               FontFamily="Arial"
               Foreground="DarkBlue"/>

        <Grid Grid.Row="1" VerticalAlignment="Top" Width="600">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DataGrid x:Name="Users" 
                  Grid.Row="2" 
                  AutoGenerateColumns="False" 
                  Height="200"
                  Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Фамилия" Binding="{Binding lastname}" Width="Auto"/>
                    <DataGridTextColumn Header="Имя" Binding="{Binding firstname}" Width="Auto"/>
                    <DataGridTextColumn Header="Роль" Binding="{Binding role}" Width="Auto"/>
                    <DataGridTextColumn Header="Логин" Binding="{Binding username}" Width="Auto"/>
                    <DataGridTextColumn Header="Заблокирован" Binding="{Binding IsLoced}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="3" 
            Orientation="Horizontal" 
            Margin="0,10,0,0"
            HorizontalAlignment="Left">

                <Button Content="Добавить пользователя"
                   Width="150" 
                   Height="30"     
                   Background="SteelBlue"    
                   Foreground="White"
                   FontWeight="Bold"
                   Cursor="Hand"   
                   BorderThickness="0"
                   Margin="0,0,10,0"  
                   Click="AddUserButton_Click"/>

                <Button Content="Разблокировать"
                  Width="120" 
                  Height="30"   
                  Background="SteelBlue"    
                  Foreground="White"
                  FontWeight="Bold"
                  Cursor="Hand"   
                  BorderThickness="0"
                  Margin="0,0,10,0" 
                  Click="ISLocedUserButton_Click"/>

                <Button Content="Удалить пользователя"
                  Width="150" 
                  Height="30"   
                  Background="Gray"    
                  Foreground="White"
                  FontWeight="Bold"
                  Cursor="Hand"   
                  BorderThickness="0" 
                  Click="DeleteUserButton_Click"/>
            </StackPanel>

        </Grid>
    </Grid>
</Page>